bi;(use-package catppuccin-theme
;  :config
  ;; 设置你喜欢的风味：'latte, 'frappe, 'macchiato, 或 'mocha (默认)
;  (setq catppuccin-flavor 'mocha) 
;  (load-theme 'catppuccin t))
(use-package gruvbox-theme
  :config
  (load-theme 'gruvbox-dark-medium t))

(provide 'theme)
